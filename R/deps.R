suppressMessages(
    c(
        library(cowplot),
        library(data.tree),
        library(feather),
        library(gridExtra),
        library(logging),
        library(lpSolve),
        library(magrittr),
        library(parallel),
        library(pipeR),
        library(RColorBrewer),
        library(Rcpp),
        library(reshape2),
        library(tidyverse),
        library(zoo)));
